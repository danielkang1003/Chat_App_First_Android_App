<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/appTheme</item>    <!-- 타이틀바 (앱 이름 나오는 바) -->
        <item name="colorPrimaryDark">@color/appThemePrimary</item>     <!-- 상태바 (최상단 시간이랑 LTE, WiFi 보여주는 바) -->
        <item name="colorAccent">@color/colorAccent</item>

<!--&lt;!&ndash;        타이틀 바 없애기&ndash;&gt;-->
<!--        <item name="windowActionBar">false</item>-->
<!--        <item name="windowNoTitle">true</item>-->
    </style>

<!--  툴바를 사용하려면 액션바를 사용하지 않는 테마를 써야함  -->
    <style name="AppThemeNo" parent="Theme.AppCompat.Light.NoActionBar">
<!--        customizing -->
        <item name="colorPrimary">@color/appTheme</item>
        <item name="colorPrimaryDark">@color/appThemePrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionBarSize">49dp</item>
        <item name="actionBarSize">49dp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
</resources>
